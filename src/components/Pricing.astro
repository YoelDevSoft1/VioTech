---
const usdRate = 4000;
const services = [
  {
    name: "Landing Page Esencial",
    price: 690000,
    usd: (690000 / usdRate).toFixed(0),
    description: "Presencia profesional y rápida para captar tus primeros clientes online.",
    features: [
      "Landing personalizada (1 sección)",
      "Dominio .com o .co 1 año",
      "Hosting seguro 12 meses",
      "Optimización móvil y velocidad",
      "Soporte y ajustes 15 días",
      "Entrega en 7 días hábiles"
    ],
    cta: "Solicitar Esencial",
    popular: false,
    color: "primary"
  },
  {
    name: "Landing Page Profesional",
    price: 990000,
    usd: (990000 / usdRate).toFixed(0),
    description: "Capta más leads con una landing multi-sección y herramientas de conversión.",
    features: [
      "Hasta 4 secciones personalizadas",
      "Dominio .com o .co 1 año",
      "Hosting seguro 12 meses",
      "Formulario avanzado (WhatsApp/CRM)",
      "Redacción optimizada y asesoría estratégica",
      "Reportes de visitas y conversiones",
      "Soporte y mejoras 30 días"
    ],
    cta: "Solicitar Profesional",
    popular: true,
    color: "primary"
  },
  {
    name: "Landing Page Premium",
    price: 1390000,
    usd: (1390000 / usdRate).toFixed(0),
    description: "Automatiza, mide y crece: automatización, analítica y marketing avanzado.",
    features: [
      "Todo lo de Profesional",
      "Animaciones y recursos interactivos",
      "Google Analytics y Facebook Pixel",
      "Integración con newsletter",
      "Automatización WhatsApp/CRM",
      "Soporte y actualizaciones 60 días"
    ],
    cta: "Solicitar Premium",
    popular: false,
    color: "secondary"
  }
];
---

<section id="pricing" class="section bg-gray-50 dark:bg-gray-800/50">
  <div class="container-custom">
    <div class="text-center max-w-3xl mx-auto mb-16">
      <span class="inline-block px-4 py-1 rounded-full bg-accent-100 dark:bg-accent-900 text-accent-600 dark:text-accent-300 font-medium text-sm mb-4">
        Planes de Landing Pages
      </span>
      <h2 class="mb-6 text-gray-900 dark:text-white">Todo incluido, precio único y soporte real</h2>
      <p class="text-gray-600 dark:text-gray-300">
        Elige el plan que más se ajusta a tu negocio. Todos los servicios incluyen dominio, hosting y atención personalizada.
      </p>
    </div>
    
    <div class="grid md:grid-cols-3 gap-8">
      {services.map((service, index) => (
        <div 
          class={`card p-6 border 
            ${service.popular ? 'border-primary-600 dark:border-primary-400 shadow-lg scale-105 z-10' : 'border-gray-200 dark:border-gray-700'} 
            slide-up relative transition-transform duration-300`}
          style={`animation-delay: ${index * 150}ms`}
        >
          {service.name === "Landing Page Profesional" && (
            <div class="absolute top-0 right-0 bg-primary-600 text-white px-4 py-1 text-sm font-semibold rounded-bl-lg rounded-tr-lg shadow-md tracking-wide">
              Más elegido
            </div>
          )}
          <h3 class="text-xl font-semibold mb-2 text-gray-900 dark:text-white">{service.name}</h3>
          <div class="mb-4">
            <span class="text-4xl font-bold text-gray-900 dark:text-white">${service.price.toLocaleString('es-CO')}</span>
            <span class="text-gray-500 dark:text-gray-400">COP</span>
            <span class="ml-2 text-sm text-gray-500 dark:text-gray-400">/ USD {service.usd}</span>
          </div>
          <p class="text-gray-600 dark:text-gray-300 mb-6">{service.description}</p>
          <ul class="space-y-3 mb-8">
            {service.features.map(feature => (
              <li class="flex items-start">
                <svg
                  class={`w-5 h-5 mr-2 shrink-0 mt-0.5
                    ${
                      service.name === "Landing Page Esencial"
                        ? "text-yellow-400"
                        : service.name === "Landing Page Profesional"
                        ? "text-primary-600"
                        : `text-${service.color}-500`
                    }
                  `}
                  fill="currentColor" viewBox="0 0 20 20">
                  <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd"></path>
                </svg>
                <span class="text-gray-600 dark:text-gray-300">{feature}</span>
              </li>
            ))}
          </ul>
          <a 
            href="#contacto" 
            class={`w-full btn
              ${service.name === "Landing Page Profesional"
                ? 'bg-primary-600 hover:bg-primary-700 text-white border-primary-600 shadow-md focus:ring-2 focus:ring-primary-300'
                : service.popular
                  ? `bg-${service.color}-600 hover:bg-${service.color}-700 text-white focus:ring-${service.color}-500 border-${service.color}-500 dark:border-${service.color}-400`
                  : 'btn-outline'
              }`}
            style="font-weight:600;font-size:1.1rem"
          >
            {service.cta}
          </a>
        </div>
      ))}
    </div>
    <div class="mt-12 text-center">
      <p class="text-gray-600 dark:text-gray-300 mb-2">¿Tu empresa necesita algo especial?</p>
      <a href="/contact" class="text-primary-600 dark:text-primary-400 font-medium hover:underline">Solicita una cotización personalizada</a>
    </div>
  </div>
</section>
