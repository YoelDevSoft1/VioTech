<style>
  @keyframes pop-in {
    0% { opacity:0; transform: scale(0.7) translateY(60px);}
    80% { opacity:1; transform: scale(1.05) translateY(-5px);}
    100% { opacity:1; transform: scale(1) translateY(0);}
  }
  .push-center-viotech {
    animation: pop-in 0.7s cubic-bezier(.26,2,.53,.97);
  }
</style>

<section id="signup" class="section">
  <div class="container-custom max-w-4xl">
    <div class="card p-8 md:p-12 border border-gray-200 dark:border-gray-700 relative">
      <div class="text-center mb-8">
        <h2 class="mb-4 text-gray-900 dark:text-white">Recibe Novedades y Consejos</h2>
        <p class="text-gray-600 dark:text-gray-300 max-w-2xl mx-auto">
          Suscríbete a nuestro boletín y recibe en tu correo las últimas noticias, tips de tecnología para tu empresa y ofertas exclusivas de VioTech Solutions.
        </p>
      </div>
      
      <form class="max-w-xl mx-auto"
        x-data="{ email: '', submitted: false, error: '' }"
        @submit.prevent="
          error = '';
          let input = $el.querySelector('#email');
          if (!input.checkValidity()) {
            error = 'Ingresa un correo válido';
            input.focus();
            return;
          }
          submitted = true;
        "
      >
        <div class="relative" x-show="!submitted">
          <div class="flex flex-col sm:flex-row gap-4">
            <div class="grow">
              <label for="email" class="sr-only">Correo electrónico</label>
              <input 
                type="email" 
                id="email" 
                x-model="email"
                placeholder="Ingresa tu correo electrónico" 
                class="w-full px-4 py-3 rounded-lg border border-gray-300 dark:border-gray-600 bg-white dark:bg-gray-800 text-gray-900 dark:text-white focus:outline-hidden focus:ring-2 focus:ring-primary-500 dark:focus:ring-primary-400"
                required
              />
              <p class="text-red-500 text-sm min-h-[1.5rem] mt-1" x-show="error" x-text="error"></p>
            </div>
            <div class="flex-shrink-0 flex items-end">
              <button type="submit" class="btn-primary whitespace-nowrap">
                Suscribirme
              </button>
            </div>
          </div>
        </div>

        <!-- PUSH compacto y centrado dentro del CARD -->
        <div
          x-show="submitted"
          x-transition
          class="absolute left-1/2 top-1/2 max-w-xs w-full -translate-x-1/2 -translate-y-1/2 bg-white dark:bg-primary-900/95 shadow-xl rounded-xl px-4 py-6 push-center-viotech border border-primary-200 dark:border-primary-700 text-center z-30"
          style="backdrop-filter: blur(2px);"
        >
          <!-- BANDERINES SVG -->
          <svg class="w-full mb-2" height="24" viewBox="0 0 340 24" fill="none" xmlns="http://www.w3.org/2000/svg">
            <g>
              <path d="M0 0 L15 24 L30 0 Z" fill="#7c3aed"/>
              <path d="M30 0 L45 24 L60 0 Z" fill="#06b6d4"/>
              <path d="M60 0 L75 24 L90 0 Z" fill="#fde047"/>
              <path d="M90 0 L105 24 L120 0 Z" fill="#f43f5e"/>
              <path d="M120 0 L135 24 L150 0 Z" fill="#22d3ee"/>
              <path d="M150 0 L165 24 L180 0 Z" fill="#818cf8"/>
              <path d="M180 0 L195 24 L210 0 Z" fill="#a3e635"/>
              <path d="M210 0 L225 24 L240 0 Z" fill="#fb7185"/>
              <path d="M240 0 L247 12 L255 0 Z" fill="#facc15"/>
            </g>
          </svg>
          <svg class="w-8 h-8 text-primary-500 mx-auto mb-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"></path>
          </svg>
          <h3 class="text-lg font-semibold text-gray-900 dark:text-white mb-1">¡Gracias por suscribirte!</h3>
          <p class="text-gray-700 dark:text-gray-200 text-center mb-2 text-sm">
            Te hemos añadido al boletín. Pronto recibirás recursos exclusivos.
          </p>
          <button
            type="button"
            class="pointer-events-auto mt-1 text-xs px-3 py-1 bg-primary-100 dark:bg-primary-800/60 rounded hover:bg-primary-200 dark:hover:bg-primary-700 text-primary-700 dark:text-primary-200 font-semibold transition"
            @click="submitted = false"
          >Cerrar</button>
        </div>
      </form>
      
      <div class="mt-8 text-center text-sm text-gray-500 dark:text-gray-400">
        Al suscribirte, aceptas nuestra <a href="#" class="text-primary-600 dark:text-primary-400 hover:underline">Política de Privacidad</a> y <a href="#" class="text-primary-600 dark:text-primary-400 hover:underline">Términos de Servicio</a>.
      </div>
    </div>
  </div>
</section>
